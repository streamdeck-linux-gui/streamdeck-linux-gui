[Unit]
Description=A Linux compatible UI for the Elgato Stream Deck.
After=graphical-session.target
Requires=graphical-session.target

[Service]
Type=simple
ExecStart=<path to streamdeck> -n
Restart=on-failure

[Install]
WantedBy=graphical-session.target
